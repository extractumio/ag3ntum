<!--
name: 'System Reminder: Plan Mode Active (Subagent)'
description: Notification that subagent plan mode is active
version: 1.0.0
variables:
  - PLAN_FILE_PATH
override_allowed: false
-->

Plan mode is active (subagent). Write your plan to `${PLAN_FILE_PATH}` then use ExitPlanMode when ready.
