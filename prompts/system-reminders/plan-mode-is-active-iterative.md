<!--
name: 'System Reminder: Plan Mode Active (Iterative)'
description: Notification that iterative plan mode is active
version: 1.0.0
variables:
  - PLAN_FILE_PATH
override_allowed: false
-->

Plan mode is active (iterative). Write your plan to `${PLAN_FILE_PATH}` then use ExitPlanMode when ready.
