# User-specific secrets configuration
# This file is created during user registration at /users/<username>/ag3ntum/secrets.yaml
#
# SECURITY: This file contains sensitive API keys and should be:
# - Readable only by the user (chmod 600)
# - Never committed to version control
# - Backed up securely
#
# These secrets are passed to the sandbox as environment variables.
# They are available inside the sandbox for skill scripts to use.
# User-specific values override global values from config/secrets.yaml.

# Environment variables available inside the sandbox
# Format: VAR_NAME: "value"
sandboxed_envs:
  # Google Gemini API key (for image generation with Gemini)
  # Get from: https://makersuite.google.com/app/apikey
  GEMINI_API_KEY: ""

  # OpenAI API key (for GPT, DALL-E, etc.)
  # Get from: https://platform.openai.com/api-keys
  OPENAI_API_KEY: ""

  # Anthropic Claude API key (for Claude API access)
  # Get from: https://console.anthropic.com/settings/keys
  ANTHROPIC_API_KEY: ""

  # Add custom environment variables below
  # They will be available in the sandbox as environment variables
  # Example:
  # MY_CUSTOM_API_KEY: "your-key-here"
  # DATABASE_URL: "postgresql://..."
